# Claim: a job redefined in the main file deep-merges over the same job from
# an include — override keys win, sibling keys from the include survive.
# Oracle-verified GitLab-exact 2026-07-21 (see test/include-merge-probe.mjs
# and the ADR-0002 amendment).
include:
  - local: lib/base.yml
build:
  script: [main-version]
